export enum TextFontWeight {
  BOLD = 'bold',
  SEMIBOLD = '500',
  NORMAL = '400',
  THINNER = '300',
  THIN = '100',
}

export enum TextAligns {
  LEFT = 'left',
  RIGHT = 'right',
  CENTER = 'center',
}

export enum AlignItems {
  FLEX_END = 'flex-end',
  FLEX_START = 'flex-start',
  CENTER = 'center',
  STRETCH = 'stretch',
  BASELINE = 'baseline',
}

export enum Colors {
  WHITE = '#ffffff ',
  CRIMSON = '#F62470',
  BLACK = '#000000',
}
